doctype html
html(lang="en")
    head
        title Motorbot Playlist
        meta(name="viewport",content="width=1128px, initial-scale=1")
        link(type='image/png',rel='icon',href='https://mb.lolstat.net/another_icon.png')
        link(href="https://fonts.googleapis.com/css?family=Open+Sans|Ubuntu",rel="stylesheet")
        link(href='/css/register.css',rel='stylesheet')
        link(href='/font-awesome-4.6.3/css/font-awesome.min.css',rel='stylesheet')
        script if (typeof module === 'object') {window.module = module; module = undefined;}
        script(src='/js/jquery-3.1.0.min.js')
        script if (window.module) module = window.module;
    body
        .sidebar
            .bg
        .steps
            .step
                .stepNumber.active 1
                .info Authenticate With Discord
                .details
                    p First step is to login with discord so that we can link your new MotorBot music account to your discord account. You will be redirected to discords secure login.
                    .button(onclick="window.location.href='/loginflow/register/discord'") Sign In with Discord
            .step
                .stepNumber 2
                .info Create Your New Account
            .step
                .stepNumber 3
                .info Enjoy